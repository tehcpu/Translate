<ripple
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/colorPrimaryDark">     <!-- ripple color -->

    <!-- for Button -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="255dp" />
            <solid android:color="@color/colorAccent"/>
        </shape>
    </item>

</ripple>